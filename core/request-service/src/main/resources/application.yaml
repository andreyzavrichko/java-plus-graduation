spring:
  application:
    name: request-service
  config:
    import: "configserver:"
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://config-server:8888}
      fail-fast: true
      retry:
        max-attempts: 6
        initial-interval: 1500
